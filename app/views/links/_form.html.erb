<%= form_for([@collection, @link], authenticity_token: true) do |f| %>
  <% if @link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@link.errors.count, "error") %> prohibited this link from being saved:</h2>

      <ul>
      <% @link.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url %>
    <%= f.url_field :url, required: true, autofocus:true, class:'center' %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
